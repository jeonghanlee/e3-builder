
ifndef VERBOSE
  QUIET := @
endif

ifdef DEBUG_SHELL
  SHELL = /bin/sh -x
endif


# Keep always the module up-to-date
define make_e3
$(QUIET)$(MAKE) -C $@/ vars
$(QUIET)#make -C $@/ init
$(QUIET)#make -C $@/ rebuild
endef
